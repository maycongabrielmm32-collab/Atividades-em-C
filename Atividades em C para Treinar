#include <stdio.h>
#include <stdlib.h>
\\Conversão de segundos

int main() {

    int segundos, hora, minuto;

    scanf("%i",&segundos);

    hora = segundos/3600;
    segundos%=3600;
    minuto = segundos/60;
    segundos%=60;

    printf("%i:%i:%i\n",hora,minuto,segundos);

    return 0;
}
